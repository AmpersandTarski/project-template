apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prototype-ingress
spec:
  tls:
    - hosts:
        - local.localhost  # fill in the definitive hostname, e.g. prototype.cs.ou.nl
  rules:
    - host: local.localhost  # fill in the definitive hostname, e.g. prototype.cs.ou.nl